/** Préfixe des routes protégées (hors landing publique sur `/`). */
export const ADMIN_BASE = '/dashboard'

/** Console d’administration plateforme (Yemma / SUPER_ADMIN). */
export const SUPER_ADMIN_BASE = '/super-admin'
