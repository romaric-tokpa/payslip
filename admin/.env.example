# API Nest (développement). Par défaut le code utilise http://localhost:3000/api/v1 si absent.
# Pour forcer le passage par le proxy Vite (vite.config.ts) :
# VITE_API_BASE_URL=/api/v1

# VITE_API_BASE_URL=http://localhost:3000/api/v1
